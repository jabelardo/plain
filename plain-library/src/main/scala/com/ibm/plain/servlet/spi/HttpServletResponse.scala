package com.ibm

package plain

package servlet

package spi

/**
 *
 */
trait HttpServletResponse

  extends HasContext

  with HasContent

  with HasBuffer

  with HasStatus

  with HasHeader

  with HasEncoding

  with HasSend

  with HasLocale

  with HasCookie

