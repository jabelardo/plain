#
# reference.conf
#

plain {

	#
	# application root directory at runtime
	#
	home = .

	#
	# if not set here this will default to 'java.io.tmpdir'
	#
	temp = ${plain.home}/temp
	
	config {

		version = "1.0.1"

		log-config-on-start = off

		print-stacktrace-on-error = off

		terminate-on-error = off
		
		terminate-on-error-exitcode = -1

	}
	
	logging {
	
		level = INFO
	
		directory = ${plain.home}/logs
	
		console {
		
			enable = on
			
			pattern = "[%date{yyyyMMddHHmmssSSS}] [%level] [%logger] %msg%n"
		
			# for not redirecting the console to a file set this value to "" or .
			file = ${plain.logging.directory}/console.log
			
			# not yet implemented
			rolling-pattern = .

		}
		
		plain-text {
		
			enable = off
			
			pattern = "[%date{yyyyMMddHHmmssSSS}] [%level] [%logger] %msg%n"
			
			file = ${plain.logging.directory}/logfile.log
			
			rolling-pattern = ${plain.logging.directory}"/archive/logfile.log.%d{yyyyMMdd}.zip"

		}		
		
		html {
		
			enable = on
			
			# this is very complex and, therefore, set inside logback-all.xml
			pattern = .
			
			file = ${plain.logging.directory}/logfile.html
			
			rolling-pattern = ${plain.logging.directory}"/archive/logfile.html.%d{yyyyMMdd}.zip"

		}		

		filter-debug-logger-names = [
			LocalActorRefProvider			
			IoWorker 
			IoConnectionActor
			ChunkingActor
			HttpClient
			HttpServer
			HttpConduit	
			SprayCanRootService	
			ExecutionDirectives	
		]
	
	}

}