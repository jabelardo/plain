#
# application.conf
#

plain.logging.level = info

plain.servlet.unpack-web-applications-recursively = on

plain.servlet.force-unpack-web-applications = on

plain.rest.default-dispatcher.routes = [ 
	{ uri = "hawtio/*", resource-class-name = com.ibm.plain.rest.resource.DirectoryResource, resource-config = { roots = [ "webapps/hawtio" ] } }
]

#
# akka-camel specific configuration 
#
akka.camel.jmx = on

